image:
  file: .devcontainer/Dockerfile

vscode:
  extensions:
    - "ms-vscode.cpptools-themes"

tasks:
  - name: Configure Bash
    init: echo PS1=$"'\${debian_chroot:+(\$debian_chroot)}\\$ '" >> ~/.bashrc